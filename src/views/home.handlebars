{{!-- <link rel="stylesheet" href="../public/css/home.style.css">
<link href="https://fonts.googleapis.com/css2?family=Ubuntu:ital,wght@0,300;0,400;0,500;1,300;1,400;1,500&display=swap" rel="stylesheet">

<h1>Products from home view:</h1>
<dir id="allCaontainer">
    <div>
        <div id="formDataContainer">
            <div>
                <h2>Load products</h2>
            </div>
            <div>
                <h3>All fields except thumbnails and Image Files are required </h3>
                    <div id="formContainer">
                        <form action="/" method="post" enctype="multipart/form-data">
                            <label for="title">Title</label><input type="text" name="title">
                            <label for="category">Category</label><input type="text" name="category" >
                            <label for="description">Description</label><input type="text" name="description" >
                            <label for="price">Price</label><input type="text" name="price" >
                            <label for="thumbnails">Thumbnails</label><input type="text" name="thumbnails">
                            <label for="photos">Image Files</label><input type="file" name="photo" accept="image/png, .jpeg, .jpg, .webp">
                            <label for="code">Code</label><input type="text" name="code" >
                            <label for="stock">Stock</label><input type="text" name="stock" >
                            <label for="status">Status</label><input type="text" name="status" >
                            <input id="loadButton" type="submit" value="Load">
                        </form>
                    </div>
            </div>
        </div>
    </div>
    <div id="productsContainer">
        <h2>Productos:</h2>

        <div id="tableContainer">
            <table>
                <tr>
                    <td>id</td>
                    <td>category</td>
                    <td>title</td>
                    <td>description</td>
                    <td>price</td>
                    <td>thumbnails</td>
                    <td>code</td>
                    <td>stock</td>
                    <td>status</td>
                </tr>
                {{#each products}}
                <tr>
                    <td>{{this._id}}</td>
                    <td>{{this.category}}</td>
                    <td>{{this.title}}</td>
                    <td>{{this.description}}</td>
                    <td>{{this.price}}</td>
                    <td>{{this.thumbnails}}</td>
                    <td>{{this.code}}</td>
                    <td>{{this.stock}}</td>
                    <td>{{this.status}}</td>
                </tr>
                {{/each}}
            </table>
        </div>
    </div>
</dir>

<script src="../public/js/home.view.js"></script> --}}


{{!-- <link rel="stylesheet" href="../public/css/home.style.css"> --}}
<link href="https://fonts.googleapis.com/css2?family=Ubuntu:ital,wght@0,300;0,400;0,500;1,300;1,400;1,500&display=swap" rel="stylesheet">

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mi Tienda de Productos</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #3498db;
            color: #fff;
            text-align: center;
            padding: 20px 0;
        }

        section {
            width: 90%;
            margin: 0 auto;
            display: flex;
            flex-direction: column;
            align-items: center;
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
        }

        form {
            width: 100%;
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
        }

        form input, form select {
            width: 48%;
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        form button {
            width: 100%;
            padding: 10px;
            background-color: #3498db;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        table {
            width: 90%;
            margin-top: 20px;
            border-collapse: collapse;
        }

        table, th, td {
            border: 1px solid #ccc;
        }

        th, td {
            padding: 10px;
            text-align: center;
        }

        th {
            background-color: #3498db;
            color: #fff;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        .edit-btn, .delete-btn{
            padding: 5px 10px;
            height: 30px;
            width: 50px;
            background-color: #3498db;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 2px auto;
            margin-bottom: 2px;
        }

        .delete-btn {
            background-color: #e74c3c;
        }

        .edit-btn {
            background-color: #27ae60;
        }

        .filter-btn{
            background-color: #3498db;
            padding: 5px 10px;
            height: 50px;
            width: 100px;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 2px auto;
            margin-bottom: 2px;
        }

        .titleProducts{
            width: 100%;
            display: flex;
            flex-direction: row;
            justify-content:space-between;
            align-items: baseline;
        }
    </style>
</head>
<body>
    <header>
        <h1>Products from home view</h1>
    </header>

    <section>
        <h2>Add products</h2>
        <h3>All fields except thumbnails and Image Files are required </h3>
        <form action="/" method="post" enctype="multipart/form-data" id="newProdForm">
            <input type="text" name="title" placeholder="Title" >
            <input type="text" name="category" placeholder="Category" >
            <input type="text" name="description" placeholder="Description" >
            <input type="text" name="price" placeholder="Price" >
            <input type="text" name="thumbnails"placeholder="Thumbnails" >
            <input type="file" name="photo" accept="image/png, .jpeg, .jpg, .webp" placeholder="Photos" >
            <input type="text" name="code" placeholder="Code" >
            <input type="text" name="stock" placeholder="Stock" >
            <input type="text" name="status" placeholder="Status">
            <input id="loadButton" type="submit" value="Load">
        </form>
        <div class="titleProducts">
            <h2>Products list</h2>
            {{#if filter}}
                <button class="filter-btn">Show my products</button>
            {{/if}}
            
        </div>
        <table>
            <tr>
                <th>ID</th>
                <th>Categoría</th>
                <th>Título</th>
                <th>Descripción</th>
                <th>Precio</th>
                <th>Miniaturas</th>
                <th>Código</th>
                <th>Stock</th>
                <th>Estado</th>
                <th>Acciones</th>
            </tr>
            {{#each products}}
                <tr>
                    <td>{{this._id}}</td>
                    <td>{{this.category}}</td>
                    <td>{{this.title}}</td>
                    <td>{{this.description}}</td>
                    <td>${{this.price}}</td>
                    <td>{{this.thumbnails}}</td>
                    <td>{{this.code}}</td>
                    <td>{{this.stock}}</td>
                    <td>{{this.status}}</td>
                    {{#if this.render}}
                        <td>
                            <a href="/product/modify/{{this._id}}"><button class="edit-btn">Edit</button></a>
                        </td>
                    {{/if}}

                </tr>
                {{/each}}
        </table>
    </section>
</body>
</html>




<script src="../public/js/home.view.js"></script>